import React from "react";
import styles from "./MainContainer.module.scss";

const MainContainer = () => {
  return (
    <div className={styles.mainContainer}>
      <p>Welcome to Avini</p>
    </div>
  );
};

export default MainContainer;
